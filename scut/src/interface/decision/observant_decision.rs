//! The names of these Decision implementations are faily arbitrary!
//!
//! This Decision implementation implements `predict_turn`, using the enemy turns available in RemoteStorage to determine what turn it must be

pub struct ObservantDecision {}
